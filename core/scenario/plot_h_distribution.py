
def plot_h_distribution(args):
    '''
    Refer to scenario/fit.py, then load `channel_io.npz`.
    Extract h then scatter plot real and imag part of h.
    Also, sample args.n_prep h_hat from model at `channel.pt`. Then scatter plot the real and imag part of h_hat.
    Save the plot.

    Also, in a separate file, plot the channel.pt
    '''
